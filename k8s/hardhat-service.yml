apiVersion: v1
kind: Service
metadata:
  name: hardhat-service
spec:
  type: NodePort
  selector:
    app: hardhat
  ports:
    - protocol: TCP
      port: 8545
      targetPort: 8545
      nodePort: 30545
